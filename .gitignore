# ── Python ───────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/

# ── ML data & checkpoints ────────────────────────────────
# Raw and processed images stay local — too large for git.
# Splits (train/val/test CSVs) ARE committed; they're small
# and define the dataset split for reproducibility.
ml/data/raw/
ml/data/processed/
ml/data/external_splits/
ml/runs/
ml/model_cache/
ml/models/*.pt
ml/models/**/*.pt
ml/models/**/*.pth
ml/models/**/*.safetensors
ml/models/**/*.bin
*.pt
*.pth
*.safetensors
*.ckpt
*.onnx
*.tflite
checkpoints/
artifacts/
staging/
cloud-staging/
.cloud-staging/

# ── Environment / secrets ────────────────────────────────
.env
.env.*
!.env.example
.kaggle/
kaggle.json
.c*dex/
*.pem
*.key
*.p12
*.pfx
*.mobileprovision
*.keystore
*.jks
google-service-account*.json
service-account*.json
*-service-account.json
credentials.json
secrets.json

# ── Mobile ───────────────────────────────────────────────
node_modules/
.expo/

# ── OS artifacts ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE artifacts ────────────────────────────────────────
.idea/
.vscode/
!.vscode/
!.vscode/settings.json

docs/
AGENTS.md
PROJECT.md
README.md
**/README.md
mobile/DESIGN.md

# Keep private docs ignored while allowing the intentionally public evidence set.
!docs/
docs/*
!docs/PRIVACY.md
!docs/MODEL_CARD.md
!/README.md

# ── MCP config (contains project ref) ───────────────────
.mcp.json

# â”€â”€ Expo/EAS credentials â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
.eas/credentials*

# ── Supabase ─────────────────────────────────────────────
supabase/

# ── Logs ─────────────────────────────────────────────────
*.log
.gstack/
