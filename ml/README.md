# ML

Phase 1: supervised image classifier (PyTorch).
Phase 2 (deferred): RL for image-quality / confidence thresholding.

## Layout
- `data/` — dataset loaders
- `models/` — model architectures + checkpoints
- `training/` — train/eval scripts
- `inference/` — load + predict helpers used by backend
